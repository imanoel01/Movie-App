@model List<Vidly.Models.Movie>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies List</h2>

@if (Model.Count() < 1)
{
    <h2> No Movies Currently</h2>
}

else
{


        @*<table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th> Movie</th>
                        <th>Genre</th>
                    </tr>
                </thead>


                <tbody>
                    <tr>
                        <td>
                            @Html.ActionLink(movie.Title, "Detail", "Movies", new { id = movie.Id }, null)
                        </td>

                        <td>
                            @Html.DisplayText(movie.Genre.Name)
                        </td>
                    </tr>
                </tbody>
            </table>*@

        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Movie</th>
                    <th>Genre</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var movie in Model)
                {
                    <tr>

                        <td>@Html.ActionLink(movie.Title, "Detail", "Movies", new { id = movie.Id }, null)</td>
                        <td>@movie.Genre.Name</td>
                    </tr>

                }
            </tbody>
        </table>
    
}